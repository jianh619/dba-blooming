# Copy this file to .env and fill in real values before running docker compose.
# NEVER commit .env to version control.

# PostgreSQL superuser password (used by Patroni and PgBouncer)
POSTGRES_PASSWORD=

# Replication user password (used between primary and replicas)
REPLICATION_PASSWORD=
